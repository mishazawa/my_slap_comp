SEED = 42

LIGHT = [-0.6, -0.6, 0.5]

MASK_SMOOTH_WIDTH = 10
MASK_SMOOTH_HEIGHT = 10
PAPER_SCALE = 5
SHADOW_COLOR = (0, 0, 0)  # Moved from filter config
SHADOW_INTENSITY = 0.8  # Moved from filter config
SHADOW_LIMIT = 3
OUTLINE_THICKNESS = 2  # Matches filter config
OUTLINE_COLOR = (0.9, 0.7, 0.7)  # Moved from filter config
NOISE_SCALE = 0.05  # Moved from filter config
PAPER_STRENGTH = 0.5  # Moved from filter config
